FROM josimarcamargo/ticketfy:base AS gem_installer

COPY Gemfile Gemfile.lock ./

RUN bundle install --path=/vendor/bundler --deployment --jobs 4 --retry 3 && \
    bundle clean

FROM busybox:musl

COPY --from=gem_installer  /vendor/bundler  /vendor/bundler
